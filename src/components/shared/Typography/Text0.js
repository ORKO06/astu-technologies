import React from "react";

const Text0 = ({ children }) => {
  return (
    <h2
      style={{
        lineHeight: "20px",
        fontSize: "20px",
        fontWeight: 400,
      }}
    >
      {children}
    </h2>
  );
};

export default Text0;
